rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY — wide open. Do not ship to production.
    // Replace with auth-based rules before release.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
