package pl.akp.healthybite.ui.theme

import androidx.compose.material3.Typography

/**
 * Uses the Material 3 default type scale (Roboto, standard sizes & weights).
 * To customise, pass overrides for individual text styles such as
 * `bodyLarge`, `titleMedium`, etc. to the Typography() constructor.
 */
val AppTypography = Typography()

